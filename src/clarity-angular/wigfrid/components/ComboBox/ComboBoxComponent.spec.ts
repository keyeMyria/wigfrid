import {
    beforeEachProviders,
    describe,
    expect,
    it,
    inject
} from '@angular/core/testing';
import { ComboBoxComponent } from './ComboBoxComponent';

beforeEachProviders(() => [ComboBoxComponent]);

describe('Ahri: ComboBoxComponent', () => {
    // it('should create the ComboBoxComponent', () => {
    //     inject([ComboBoxComponent], (app: ComboBoxComponent) => {
    //         expect(app).toBeTruthy();
    //     })
    // });

});
